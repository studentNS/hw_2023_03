server:
  port: 8083

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true

spring:
  application:
    name: docker-service-book-library-test-client
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true